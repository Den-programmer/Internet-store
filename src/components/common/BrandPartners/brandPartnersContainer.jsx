import { connect } from 'react-redux'
import BrandPartners from './brandPartners'

const mapStateToProps = (props) => ({

})

const BrandPartnersContainer = connect(mapStateToProps, {})(BrandPartners)

export default BrandPartnersContainer