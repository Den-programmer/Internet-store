import { connect } from 'react-redux'
import ContactPage from './contactPage'

const mapStateToProps = (state) => ({

})

const ContactPageContainer = connect(mapStateToProps, {})(ContactPage)

export default ContactPageContainer