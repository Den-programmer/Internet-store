import { connect } from 'react-redux'
import LoginPanel from './loginPanel'

const mapStateToProps = (state) => ({

})

const LoginPanelContainer = connect(mapStateToProps, {})(LoginPanel)

export default LoginPanelContainer